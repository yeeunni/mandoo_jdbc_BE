spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mandoo_dev
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always  # 항상 초기화하도록 설정
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

    properties:
      hibernate:
        format_sql: true

    defer-datasource-initialization: true
    generate-ddl: true

upload:
  path: /Users/kang-minyoung/Desktop/mandoo_Frontend/src/images

logging:
  level:
    root: INFO  # INFO 이상의 레벨을 출력하도록 설정
    mandooparty.mandoo: DEBUG  # 특정 패키지에 대해 DEBUG 로그를 출력하도록 설정
